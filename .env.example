# Copy this file to .env and fill in your Neon connection string.
# The DATABASE_URL is read ONLY by the /api serverless functions — never exposed to the browser.
DATABASE_URL="postgresql://YOUR_USER:YOUR_PASSWORD@YOUR_HOST/neondb?sslmode=require"
